class ListItem {
  bool isSelected;

  ListItem({required this.isSelected});
}

List<ListItem> yourList = [
  ListItem(isSelected: false),
  ListItem(isSelected: false),
  ListItem(isSelected: false),
  // Add more items as needed
];
